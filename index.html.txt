<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>NEXUS - Aquarium Monitor</title>
    <script src="https://gstatic.com"></script>
    <script src="https://gstatic.com"></script>
    <style>
        body { background: #0f172a; color: #f8fafc; font-family: sans-serif; display: flex; flex-direction: column; align-items: center; }
        .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 50px; }
        .card { background: #1e293b; padding: 30px; border-radius: 15px; border-top: 4px solid #38bdf8; text-align: center; min-width: 200px; box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1); }
        h1 { color: #38bdf8; margin-top: 30px; }
        .value { font-size: 3rem; font-weight: bold; margin: 10px 0; }
        .unit { color: #94a3b8; }
        .status { margin-top: 20px; color: #4ade80; font-size: 0.8rem; }
    </style>
</head>
<body>
    <h1>NEXUS AQUARIUM MONITOR</h1>
    <div class="status" id="db-status">Conectando a la nube...</div>

    <div class="grid">
        <div class="card">
            <h3>TEMPERATURA</h3>
            <div class="value" id="temp">--</div>
            <div class="unit">°Celsius</div>
        </div>
        <div class="card" style="border-top-color: #fbbf24;">
            <h3>PH AGUA</h3>
            <div class="value" id="ph">--</div>
            <div class="unit">Escala 0-14</div>
        </div>
        <div class="card" style="border-top-color: #a855f7;">
            <h3>SOLIDOS (TDS)</h3>
            <div class="value" id="tds">--</div>
            <div class="unit">PPM</div>
        </div>
    </div>

    <script>
        // --- CONFIGURACIÓN FIREBASE (Copia esto de tu consola de Firebase) ---
        const firebaseConfig = {
            databaseURL: "https://firebaseio.com"
        };
        
        firebase.initializeApp(firebaseConfig);
        const db = firebase.database();

        // Escuchar cambios en tiempo real
        db.ref('monitoreo').on('value', (snapshot) => {
            const data = snapshot.val();
            document.getElementById('temp').innerText = data.temp.toFixed(1);
            document.getElementById('ph').innerText = data.ph.toFixed(1);
            document.getElementById('tds').innerText = data.tds;
            document.getElementById('db-status').innerText = "SISTEMA ONLINE - DATOS EN TIEMPO REAL";
        });
    </script>
</body>
</html>
